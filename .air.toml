[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  include_ext = ["go", "html", "sql"]
  exclude_dir = ["tmp", "vendor", "internal/db"]
  delay = 500

[log]
  time = false

[color]
  build = "yellow"
  runner = "green"
